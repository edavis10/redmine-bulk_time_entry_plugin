<%= label_for_field :issue_id, rnd %>
<% if @issues.nil? || @issues.empty? %>
<em><%= l(:label_no_data) %></em>
<% else %>
<%= select "time_entries[#{rnd}]", 'issue_id', @issues.collect { |i| [i.to_s,  i.id] }, { :include_blank => true } %>&nbsp;
<% end %>
<%# f.text_field :issue_id, :size => 6 %>
